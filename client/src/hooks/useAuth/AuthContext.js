//Normally this would all be one file, but it's set up like this to ensure Fast Refresh works.

import { createContext } from "react";

const AuthContext = createContext(null);

export { AuthContext };
