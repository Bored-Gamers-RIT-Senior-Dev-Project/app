/**
 * Encode image as WEBP, discarding any metadata
 * @param {Buffer} image an image to attempt to encode
 * @return {Buffer | null} The image, encoded as WEBP, or null if it couldn't be converted
 */
const encodeImage = (image) => {

}

module.exports = {}